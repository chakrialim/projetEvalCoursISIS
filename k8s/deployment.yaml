# Generated by Containerization Assist using AI.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: isis-eval
spec:
  replicas: 1
  selector:
    matchLabels:
      app: isis-eval
  template:
    metadata:
      labels:
        app: isis-eval
    spec:
      imagePullSecrets:
        - name: gitlab-registry
      containers:
        - name: isis-eval
          image: isis_eval:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: isis-eval-config
            - secretRef:
                name: isis-eval-secrets
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 40
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "500m"
              memory: "1Gi"
